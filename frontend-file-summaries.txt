{
  "frontend/src/types/declarations.d.ts": "TypeScript module declarations for CDN-loaded libraries (pdf-lib, pdfjs-dist, jszip), with the `addPage` method updated to accept either a `PDFPage` object or a `[number, number]` size tuple.",
  "frontend/src/modules/ImageEditor/ImageUploadPanel.tsx": "Image upload panel with file picker and camera capture, using relative import path for the camera hook instead of the module alias.",
  "frontend/src/modules/PDFConverter/CreatePDFFromImages.tsx": "PDF creation from images component with camera support (using native `getUserMedia`), enhance/compress toggles, and ZIP download using `data` field in ZipEntry.",
  "frontend/src/modules/PDFConverter/PageManager.tsx": "PDF page manager that reads bytes from `PDFFile.bytes` if available, falling back to `PDFFile.file.arrayBuffer()`, fixing the missing `bytes` property error.",
  "frontend/src/modules/PDFConverter/usePDFOperations.ts": "PDF operations hook with both new API (`files`, `addFiles`, `removeFile`) and legacy aliases (`pdfFiles`, `addPDFs`, `removePDF`, `removePages`), storing `bytes` on PDFFile for PageManager compatibility, and tracking merge/split/create results for ZIP download.",
  "frontend/src/modules/ImageConverter/useImageConversion.ts": "Image conversion hook with batch processing, format conversion, resize, filters, and ZIP download using the shared `@/lib/jszip` utility (refactored from inline CDN loading to use the centralized helper).",
  "frontend/src/modules/ImageConverter/ConversionSettings.tsx": "Conversion settings panel with format selection, quality slider, resize options (dimensions or percentage), and filter presets.",
  "frontend/src/modules/ImageConverter/BeforeAfterPreview.tsx": "Interactive before/after slider preview component for comparing original and converted images with drag-based reveal.",
  "frontend/src/modules/ImageConverter/ExportPanel.tsx": "Export panel showing conversion progress, batch convert button, individual download buttons per converted image, and a ZIP download button for all converted images with loading states.",
  "frontend/src/modules/ImageConverter.tsx": "Image Converter with four numbered tabs (Upload → Settings → Preview → Export); Upload, Settings, and Preview tabs each have a \"Next\" button to advance to the next tab, with the Upload tab's Next button disabled when no images are loaded, and the Export tab shows existing download controls.",
  "frontend/src/index.css": "Global stylesheet defining the OKLCH design system tokens for light/dark modes, component styles (glass-card, tool-btn, upload-zone) using plain CSS instead of `@apply` with opacity modifiers to avoid PostCSS resolution errors, and animation keyframes.",
  "frontend/src/App.tsx": "Main app component updated with the `video-downloader` TabId variant, imports the VideoDownloader module, and conditionally renders it when the video-downloader tab is active alongside the existing five modules.",
  "frontend/src/components/TabNavigation.tsx": "Horizontal scrollable tab navigation bar now with 6 tabs including the new \"Video Downloader\" tab (Download icon), maintaining the same active-state styling and description display as the other tabs.",
  "frontend/src/modules/PassportPhotoConverter/usePassportPhoto.ts": "Restored original hook API with `state` containing `selectedPreset`, `processedDataUrl`, `textConfig`, `exportFormat`, `originalImage`; exports `SIZE_OPTIONS`, `TextConfig`, `mmToPx`, `A4_WIDTH_MM`, `A4_HEIGHT_MM`, `A4_MARGIN_MM`, `PHOTO_GAP_MM`; `renderA4Canvas` now accepts a canvas argument; `getIndividualPhotoBlobs` returns `Promise<Blob[]>`.",
  "frontend/src/modules/PassportPhotoConverter/CopyGridPanel.tsx": "Canvas-based A4 sheet preview accepting `canvasRef?: React.RefObject<HTMLCanvasElement | null>` to share the canvas with the parent for export.",
  "frontend/src/modules/PassportPhotoConverter/TextCustomizationPanel.tsx": "Text overlay customization panel using the correct `TextConfig` field names (`content` instead of `text`, `align` instead of `alignment`) and `onUpdate` prop name matching the parent component.",
  "frontend/src/modules/PassportPhotoConverter/ExportPanel.tsx": "Export panel for passport photos with `canvasRef: React.RefObject<HTMLCanvasElement | null>` (nullable), PNG/PDF download, print, and ZIP download using `data` field in ZipEntry.",
  "frontend/src/modules/PassportPhotoConverter.tsx": "Main Passport Photo Converter module passing `onUpdate` (not `onChange`) to `TextCustomizationPanel`, matching the restored prop interface.",
  "frontend/src/modules/BackgroundRemover/useBackgroundRemover.ts": "Restored original hook API with `state`, `loadImage(img, dataUrl)`, `updateBrushState`, `updateOutputMode`, `updateBgColor`, `updateZoom`, `updateRotation`, `updateCropBox`, `saveMaskSnapshot`, `undoLastStroke`, `removeBackground`, `autoDetectBackground`, `applyCrop`, `reset`, and `setResultDataUrl` — all exported types (`BrushState`, `BrushMode`, `OutputMode`) included.",
  "frontend/src/modules/BackgroundRemover/CropOverlay.tsx": "SVG-based crop overlay component with draggable corner/edge handles, rule-of-thirds grid lines, and dark outside-crop masking for the background remover canvas.",
  "frontend/src/modules/BackgroundRemover/BrushToolPanel.tsx": "Brush tool control panel with activate toggle, three size options (S/M/L), paint/erase mode toggle, and undo button.",
  "frontend/src/modules/BackgroundRemover/RemovalPanel.tsx": "Background removal panel with auto-detect (flood-fill), output mode selector (transparent/custom color), color picker with presets, and Remove Background action button.",
  "frontend/src/modules/BackgroundRemover/BeforeAfterPreview.tsx": "Interactive before/after slider comparison component for background remover showing original vs result with checkerboard transparency pattern.",
  "frontend/src/modules/BackgroundRemover/ExportPanel.tsx": "Export panel for background remover using the original props API (`resultDataUrl`, `outputMode`, `bgColor`, `zoom`, `onZoomChange`, `onReset`, `onAddBackgroundImage`) with ZIP download using `data` field in ZipEntry.",
  "frontend/src/modules/BackgroundRemover.tsx": "Main Background Remover module with file upload, dual-canvas (image + mask overlay), brush drawing with custom cursor, crop mode with overlay, rotation controls, and before/after comparison.",
  "frontend/src/modules/ImageEditor.tsx": "Main Image Editor module wired with interactive CropOverlay (activated from CropTool, applies crop by redrawing to a temp canvas) and TextLayerOverlay (drag/rotate handles for placed text layers), plus click-to-place text mode with a canvas click handler and placement hint bar.",
  "frontend/src/modules/PDFConverter.tsx": "PDF Converter module using legacy hook aliases (`pdfFiles`, `addPDFs`, `removePDF`, `removePages`) that now exist on `UsePDFOperationsReturn`.",
  "frontend/src/lib/jszip.ts": "Shared JSZip utility that lazy-loads JSZip from CDN with singleton promise caching, provides `downloadAsZip` (accepts `ZipEntry[]` with `data` field) and `dataUrlToBlob` helpers.",
  "frontend/src/modules/PDFConverter/MergeSplitTools.tsx": "Merge/Split tools panel accepting legacy props (`pdfFiles`, `onMerge`, `onSplit`) with ZIP download buttons using `data` field in ZipEntry.",
  "frontend/src/modules/PDFConverter/PDFFileList.tsx": "New component for uploading and listing PDF files with drag-and-drop support, file picker, per-file page count display, and individual remove buttons.",
  "frontend/src/modules/ImageEditor/CropOverlay.tsx": "New interactive crop overlay component for the Image Editor with a draggable/resizable rectangle (8 handles), rule-of-thirds grid, dark dimming outside the crop region, a size indicator, and Apply/Cancel buttons that commit or dismiss the crop.",
  "frontend/src/modules/ImageEditor/TextLayerOverlay.tsx": "New interactive text layer overlay that renders bounding boxes with corner handles and a rotation handle (with stem line) for each text layer; supports drag-to-move and drag-to-rotate interactions via mouse events, positioned absolutely over the canvas.",
  "frontend/src/modules/ImageEditor/CropTool.tsx": "Updated Crop tool panel with a prominent \"Start Interactive Crop\" button that activates the canvas overlay, an active-state hint message, quick aspect-ratio preset buttons, and a Reset Crop button.",
  "frontend/src/modules/ImageEditor/TextOverlay.tsx": "Updated Text Overlay panel with a \"Click to Place on Canvas\" button that triggers click-to-place mode (passing the current text config to the parent), an \"Add to Center\" fallback, and a layer list showing position/rotation info with selection highlighting.",
  "frontend/src/modules/VideoDownloader.tsx": "New Video Downloader module that accepts YouTube or Facebook URLs, fetches YouTube video metadata (title, thumbnail, duration, channel) via the YouTube Data API v3, displays a result panel with quality/format download options, and shows informational notices about platform download restrictions — styled with the existing glass-card/tool-btn design system."
}