{
  "frontend/src/types/declarations.d.ts": "TypeScript module declarations for CDN-loaded libraries (pdf-lib, pdfjs-dist, jszip), with the `addPage` method updated to accept either a `PDFPage` object or a `[number, number]` size tuple.",
  "frontend/src/modules/ImageEditor/ImageUploadPanel.tsx": "Image upload panel with file drag-and-drop and file-picker only — camera capture button and all getUserMedia/camera logic removed, retaining only file-based upload flows.",
  "frontend/src/modules/PDFConverter/CreatePDFFromImages.tsx": "PDF creation from images component with file drag-and-drop upload only (camera access removed), image grid preview, enhance/compress toggles, and a Create & Download PDF button using pdf-lib.",
  "frontend/src/modules/PDFConverter/PageManager.tsx": "PDF page manager updated to use `file.file.arrayBuffer()` instead of the removed `file.bytes` field, and typed `loadThumbnails` parameter as `PDFFile | undefined` to fix the implicit-any error.",
  "frontend/src/modules/PDFConverter/usePDFOperations.ts": "Rewritten PDF operations hook with updated `PDFFile` type (using `type` instead of `fileType`, no `bytes`/`pageCount`), dynamic CDN loading for both pdf-lib and PDF.js returning typed `any` to avoid undefined errors, and full merge/split/reorder/convert workflows.",
  "frontend/src/modules/ImageConverter/useImageConversion.ts": "Restored original ImageFile type with `preview` field, ConvertedImage, ConversionProgress, OutputFormat, FilterType exports, and fixed downloadAsZip to use `filename` field.",
  "frontend/src/modules/ImageConverter/ConversionSettings.tsx": "Conversion settings panel (format, quality, resize, filter controls) with \"Back\" and \"Next\" navigation buttons at the bottom that call `onBack`/`onNext` props.",
  "frontend/src/modules/ImageConverter/BeforeAfterPreview.tsx": "Before/after slider preview component with \"Back\" and \"Next\" navigation buttons at the bottom that call `onBack`/`onNext` props.",
  "frontend/src/modules/ImageConverter/ExportPanel.tsx": "Export panel with convert-all, per-image download, ZIP download controls, and a \"Back\" navigation button at the bottom (no Next since it is the final step) that calls the `onBack` prop.",
  "frontend/src/modules/ImageConverter.tsx": "Main Image Converter module with tab navigation (Upload/Settings/Preview/Export), a persistent left preview panel, and a `goNext`/`goBack` handler passed down to each sub-panel so they can drive linear step-by-step navigation.",
  "frontend/src/index.css": "Global stylesheet with OKLCH design tokens for light/dark modes, existing component utilities (glass-card, tool-btn, upload-zone), and new Multimedia Studio-specific classes (multimedia-bg, mm-card, mm-upload-zone, mm-input, mm-card-inner) for the dark glassmorphism theme.",
  "frontend/src/App.tsx": "Main app component with ThemeProvider, tabbed navigation for the original 5 tools, and a `showMultimedia` state that switches to the new MultimediaApp when the Multimedia button in the header is clicked.",
  "frontend/src/components/TabNavigation.tsx": "Horizontal scrollable tab navigation bar with 5 tabs (Image Editor, PDF Converter, Image Converter, Passport Photo, BG Remover) — Video Downloader tab removed.",
  "frontend/src/modules/PassportPhotoConverter/usePassportPhoto.ts": "Passport photo hook with extended TextConfig interface that now includes textBgEnabled, textBgColor, textBgOpacity, textShadowEnabled, textShadowColor, textShadowBlur, textShadowOffsetX, and textShadowOffsetY fields; renderA4Canvas updated to draw background rect and apply canvas shadow properties when those features are enabled.",
  "frontend/src/modules/PassportPhotoConverter/CopyGridPanel.tsx": "A4 grid canvas panel that now draws a filled background rectangle (with configurable color and opacity) behind text and applies Canvas 2D shadow properties (shadowColor, shadowBlur, shadowOffsetX, shadowOffsetY) before rendering text, resetting shadow state after each draw to prevent bleed-through.",
  "frontend/src/modules/PassportPhotoConverter/TextCustomizationPanel.tsx": "Text customization panel with original font/color/bold/italic/align controls plus two new collapsible sections — \"Text Background\" (color picker + opacity slider with live preview swatch) and \"Text Shadow\" (color picker, blur radius slider, horizontal/vertical offset sliders, and a live CSS shadow preview), each gated by a Switch toggle.",
  "frontend/src/modules/PassportPhotoConverter/ExportPanel.tsx": "Export panel updated to support PNG, PDF, and GIF format options; GIF export uses canvas.toDataURL('image/gif') with PNG fallback; PDF export uses maximum quality (1.0); all three format buttons are displayed in the format selector.",
  "frontend/src/modules/PassportPhotoConverter.tsx": "Main Passport Photo Converter with named export, using original hook API, onError handler on preview image, and correct ExportPanel default import.",
  "frontend/src/modules/BackgroundRemover/useBackgroundRemover.ts": "Background remover hook with default brush size changed to 20px (within the new 4–100 range), `drawMaskToCanvas` documented as stroke-end-only to avoid mid-stroke re-renders, and all other state/logic (load, crop, erase, undo, reset) unchanged.",
  "frontend/src/modules/BackgroundRemover/CropOverlay.tsx": "SVG-based crop overlay component with draggable corner/edge handles, rule-of-thirds grid lines, and dark outside-crop masking for the background remover canvas.",
  "frontend/src/modules/BackgroundRemover/BrushToolPanel.tsx": "Brush tool control panel with activate toggle, paint/unpaint mode toggle, a continuous thickness slider (4–100px) with live numeric display and a scaled brush preview circle, undo/clear buttons, and a prominent \"Erase Selected Area\" button; touch event propagation is stopped at the panel level.",
  "frontend/src/modules/BackgroundRemover/RemovalPanel.tsx": "Background removal panel importing `OutputMode` from the updated useBackgroundRemover hook, with auto-detect, output mode selector, color picker, and Remove Background button.",
  "frontend/src/modules/BackgroundRemover/BeforeAfterPreview.tsx": "Before/after comparison slider with default export, onError/onLoad handlers on both images, and graceful error fallbacks using the original prop names (originalDataUrl, resultDataUrl).",
  "frontend/src/modules/BackgroundRemover/ExportPanel.tsx": "Export panel importing `OutputMode` from the updated useBackgroundRemover hook, with zoom controls, background image upload, format selector (PNG/JPG), download, ZIP download, and reset buttons.",
  "frontend/src/modules/BackgroundRemover.tsx": "Main Background Remover module with smooth brush painting via line-interpolation between pointer positions, a custom circular brush cursor overlay that scales with the selected brush size, `drawMaskToCanvas` called only at stroke-end to prevent re-render jitter, and all existing crop/export/comparison functionality preserved.",
  "frontend/src/modules/ImageEditor.tsx": "Main Image Editor module with canvas editing, crop overlay, text layer overlay, and upload panel — `hasImage` prop removed from both `ImageUploadPanel` usages to match the updated component interface.",
  "frontend/src/modules/PDFConverter.tsx": "Main PDF Converter module with four tabs (Scanner, Files, Merge/Split, Create) wired to the updated `usePDFOperations` hook; the old Convert tab and its `ConvertDownload` component with mismatched props are removed.",
  "frontend/src/lib/jszip.ts": "Shared JSZip utility that lazy-loads JSZip from CDN with singleton promise caching, provides `downloadAsZip` (accepts `ZipEntry[]` with `data` field) and `dataUrlToBlob` helpers.",
  "frontend/src/modules/PDFConverter/MergeSplitTools.tsx": "Completely redesigned Merge/Split panel with individual checkboxes, Select All control, HTML5 drag-to-reorder thumbnails with visual drop indicators, remove/restore toggles, and a Merge & Download button with loading/success states.",
  "frontend/src/modules/PDFConverter/PDFFileList.tsx": "File list component for the PDF Converter Files tab with individual checkboxes, Select All toggle, visual selection highlighting, and a Convert to PDF button that operates on selected files (or all files if none selected), disabled when the list is empty.",
  "frontend/src/modules/ImageEditor/CropOverlay.tsx": "New interactive crop overlay component for the Image Editor with a draggable/resizable rectangle (8 handles), rule-of-thirds grid, dark dimming outside the crop region, a size indicator, and Apply/Cancel buttons that commit or dismiss the crop.",
  "frontend/src/modules/ImageEditor/TextLayerOverlay.tsx": "New interactive text layer overlay that renders bounding boxes with corner handles and a rotation handle (with stem line) for each text layer; supports drag-to-move and drag-to-rotate interactions via mouse events, positioned absolutely over the canvas.",
  "frontend/src/modules/ImageEditor/CropTool.tsx": "Updated Crop tool panel with a prominent \"Start Interactive Crop\" button that activates the canvas overlay, an active-state hint message, quick aspect-ratio preset buttons, and a Reset Crop button.",
  "frontend/src/modules/ImageEditor/TextOverlay.tsx": "Updated Text Overlay panel with collapsible \"Text Background\" section (toggle + color picker + preview swatch) and collapsible \"Text Shadow\" section (toggle + color picker + blur radius slider 0–20px + horizontal/vertical offset sliders -10 to 10px + live CSS preview), all wired into the buildLayer config passed to onAdd/onStartPlacing.",
  "frontend/src/modules/VideoDownloader.tsx": "Refactored Video Downloader module with fixed download logic — quality options now act as selectors with a single Download button, all download actions are wrapped in try-catch, videoId null-checks prevent undefined errors, Facebook oEmbed fetch is wrapped in async try-catch with graceful fallback to opening the original URL, a separate `downloadError` state displays styled error messages, and pop-up block detection is included.",
  "frontend/src/modules/ImageConverter/BatchUploadPanel.tsx": "Batch upload panel with drag-and-drop/file-picker, thumbnail grid, clear-all button, and a \"Next\" button at the bottom (disabled when no images are uploaded) that calls the `onNext` prop.",
  "frontend/src/modules/ImageEditor/useImageCanvas.ts": "Extended TextLayer interface with backgroundEnabled, backgroundColor, shadowEnabled, shadowColor, shadowBlur, shadowOffsetX, shadowOffsetY fields; updated canvas rendering to draw a filled background rect behind text when backgroundEnabled is true, and apply detailed shadow properties (color, blur, offsets) when shadowEnabled is true, with legacy shadow bool as fallback.",
  "frontend/src/modules/PDFConverter/ConvertDownload.tsx": "Simplified Convert & Download panel that uses the updated `PDFFile` type (with `type` field instead of `fileType`) and accepts only `files`, `isConverting`, and `onConvert` props.",
  "frontend/src/modules/PDFConverter/PDFScanner.tsx": "Complete PDF Scanner component with two separate hidden file inputs — one for \"Choose from Files\" (no capture, supports multiple/PDF) and one dedicated camera input (capture=\"environment\", image-only) that opens the native camera on mobile. Features a prominent \"Scan with Camera\" button (teal accent) alongside \"Choose from Files\", a \"Scan Next Page\" camera button in the thumbnail panel for capturing additional pages one by one, and the full existing workflow (auto edge-detection, crop overlay, brightness/contrast/darken-text adjustments, Convert to PDF, Download PDF).",
  "frontend/src/modules/PDFConverter/PDFUploadPanel.tsx": "PDF upload panel updated to use the new `PDFFile` type — removed `pageCount` display since the field no longer exists, replaced with file size display.",
  "frontend/src/components/Header.tsx": "Sticky header with logo, theme toggle, and an optional `onOpenMultimedia` prop that renders a \"Multimedia\" button to navigate to the Multimedia Studio.",
  "frontend/src/MultimediaApp.tsx": "Root component for the Multimedia Studio with a dark glassmorphism design, four-tab navigation (MP3 Cutter, Video Cutter, Video Downloader, IP Camera), theme toggle, back button, and footer with caffeine.ai attribution.",
  "frontend/src/modules/MP3Cutter.tsx": "MP3 Cutter module using Web Audio API to decode audio files (MP3/AAC/WAV/MP4/M4A), renders a waveform on Canvas with draggable teal/cyan trim handles, supports preview playback, and exports the trimmed segment as a WAV file via a built-in WAV encoder.",
  "frontend/src/modules/VideoCutter.tsx": "Video Cutter module with file upload, HTML5 video preview, draggable timeline scrubber, optional FFmpeg.wasm from CDN, and a MediaRecorder fallback; fixes the `Uint8Array<ArrayBufferLike>` Blob type error by explicitly converting to a concrete `ArrayBuffer` before passing to the Blob constructor.",
  "frontend/src/modules/VideoDownloaderMultimedia.tsx": "Standalone Video Downloader for the Multimedia Studio that fetches YouTube metadata via the YouTube Data API v3, displays thumbnail/title/channel, and opens the video in a new tab with a clear user message about browser-based saving.",
  "frontend/src/modules/IPCamera.tsx": "IP Camera module using the `useCamera` hook (imported via relative path `../camera/useCamera`), WebRTC ICE candidate gathering to detect local WiFi IP, copy-to-clipboard, and clear error/status messaging with glassmorphism styling."
}